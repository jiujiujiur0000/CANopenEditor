1.输入框有修改时点亮设备树右侧的小绿点，
2.输入框失去焦点、有修改、内容通过合规性检查（如果有的话）时自动保存，
3.只有所有修改保存成功后或文本框恢复原本内容时才熄灭绿点，
4.未失去焦点时不进行自动保存，
5.合规性检查未通过时不自动保存
6.关闭软件时，如有内容未保存，提示哪些内容未保存，因为什么原因，让用户选择放弃修改还是取消当前关闭
7.验证失败时的 UI 提示反馈：当输入框失去焦点且检查未通过时，输入框给出明显的错误视觉反馈（如红框或 ToolTip 错误提示）。
8.快捷撤销/恢复机制：未保存或验证失败时，按下 Esc 键一键恢复该输入框到修改前的合法原值，并重新评估绿点状态。
9.非文本框控件的触发时机：下拉框、复选框等在选项改变时立刻触发检查和保存，不需要等失去焦点。
10.跨焦点操作的拦截：输入不合法时点击设备树切换，允许切换但保留该设备树节点上的未保存/错误标记（绿点/红点）。
11.全局层级的状态标记：主窗口标题栏后追加 `*` 号，提示整个工作区有未保存的更改。
12.外部保存失败的兜底：物理文件保存失败时，保留绿点并在底部状态栏提示错误原因。
